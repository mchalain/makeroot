menu "System Type"

choice
    default FHS_LINUX

    prompt "System Hierarchy"
    config FHS_LSB
        bool "Linux Standard Base"
		help
			Linux Standard Base defines the hierarchy

    config FHS_ANDROID
        bool "Android system"

endchoice

config SYSTEM
    string
    default "lsb" if FHS_LSB
    default "android" if FHS_ANDROID

if FHS_LSB
source "system/tree/fhs/Config.in"
endif

endmenu
